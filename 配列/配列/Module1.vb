Module Module1

    Sub Main()
        Dim score(4) As Integer
        score(0) = 1
        score(1) = 2
        score(2) = 3
        score(3) = 4
        score(4) = 5

        Console.WriteLine("生徒1：得点は" & score(0))
        Console.WriteLine("生徒2：得点は" & score(1))
        Console.WriteLine("生徒3：得点は" & score(2))
        Console.WriteLine("生徒4：得点は" & score(3))
        Console.WriteLine("生徒5：得点は" & score(4))
    End Sub

End Module
